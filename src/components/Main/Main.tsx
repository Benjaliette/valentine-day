import { Button, MovingButton } from "../UI";
import "./Main.scss";

const Main = () => {
  return (
    <main className="main">
      <Button />
      <MovingButton />
    </main>
  );
};

export default Main;
